required:
  - prop1
properties:
  prop1:
    type: integer
  prop2:
    $ref: "#/definitions/intWrapper"
  mod:
    required:
      - name
    properties:
      name:
        type: string
      Weight of me:
        $ref: "#/definitions/AGE"
      Fear of me:
        $ref: "#/definitions/badly named array"

definitions:
  intWrapper:
    type: integer
    some:
      none:
        conforming: nonsense

  badly named array:
    type: object
    properties:
      this:
        type: string
      that:
        type: integer
      other:
        type: array
      more:
        required:
          - x
        properties:
          x: {}
          y: {}

  AGE:
    type: number

  pick from these:
    anyOf:
      - type: integer
      - type: string
      - type: object
        required:
          - some property here
        properties:
          some property here:
            type: array
            items:
              $ref: "#/definitions/AGE"

  combine these:
    allOf:
      - properties:
          name:
            type: string
      - properties:
          height:
            type: number
